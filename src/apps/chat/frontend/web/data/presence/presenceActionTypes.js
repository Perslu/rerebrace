import { makeConstantBuilder } from '../utils'

const constantBuilder = makeConstantBuilder('chat', 'presence')

export const STATE_SYNC = constantBuilder('STATE_SYNC')
export const STATE_DIFF = constantBuilder('STATE_DIFF')
/* PLOP-ACTION_TYPE_DEFINITION */


/* PLOP-FILE_END */
